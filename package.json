{
  "name": "tophaus-fidelidade",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "firebase": "10.8.1",
    "firebase-admin": "^14.3.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "tailwindcss": "^3.4.19"
  },
  "engines": {
    "node": ">=20"
  }
}

