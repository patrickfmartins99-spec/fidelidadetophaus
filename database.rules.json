{
  "rules": {
    ".read": false,
    ".write": false,

    "lojas": {
      "$unidade": {
        "usuarios_por_uid": {
          "$uid": {
            ".read": "auth != null && (auth.uid === $uid || auth.token.role === 'admin')",
            ".write": false
          }
        },

        "clientes": {
          ".read": "auth != null && (auth.token.role === 'admin' || auth.token.role === 'gerente')",
          "$cpf": {
            ".write": "auth != null && (auth.token.role === 'admin' || auth.token.role === 'gerente')",
            "almocos": {
              ".validate": "newData.isNumber() && newData.val() >= 0"
            }
          }
        },

        "config": {
          "mensagens": {
            ".read": "auth != null && (auth.token.role === 'admin' || auth.token.role === 'gerente')",
            ".write": "auth != null && (auth.token.role === 'admin' || auth.token.role === 'gerente')"
          }
        },

        "fila_mensagens": {
          ".read": false,
          ".write": false
        },

        "auditoria": {
          ".read": "auth != null && auth.token.role === 'admin'",
          ".write": false
        },

        "contadores": {
          ".read": false,
          ".write": false
        }
      }
    }
  }
}
